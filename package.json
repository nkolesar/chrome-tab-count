{
  "name": "tab-counter",
  "version": "1.0.0",
  "description": "Chrome extension to count tabs",
  "main": "background.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
} 